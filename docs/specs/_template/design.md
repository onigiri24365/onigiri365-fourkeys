# 設計: <機能名>

- **Spec**: NNNN-feature-slug
- **Status**: draft
- **最終更新**: YYYY-MM-DD
- **要件**: [requirements.md](./requirements.md)

> このファイルには **How（どう実装するか）** を書きます。
> `requirements.md` が **approved** になるまで、このファイルを書き始めてはいけません。

---

## 1. アーキテクチャ概要

<この機能がシステム全体のどこに位置し、どのコンポーネントがどう連携するか。>

処理の流れ:

```
1. <...>
2. <...>
3. <...>
```

---

## 2. データモデル

### 新規テーブル

**`table_name`**

| カラム | 型 | NULL | 説明 |
|---|---|---|---|
| `id` | bigint | no | PK |
| `<column>` | `<type>` | | |
| `created_at` | datetime | no | |
| `updated_at` | datetime | no | |

- **インデックス**: <...>
- **一意制約**: <...>
- **外部キー**: <...>

### 既存テーブルの変更

| テーブル | 変更内容 | 理由 |
|---|---|---|
| | | |

### マイグレーション方針

<データの後方互換、既存レコードの扱い、ロールバック可否。>

---

## 3. API エンドポイント

### `GET /api/v1/<path>`

**クエリパラメータ**

| 名前 | 型 | 必須 | 既定値 | 説明 |
|---|---|---|---|---|
| `from` | date | yes | | 集計開始日（JST） |
| `to` | date | yes | | 集計終了日（JST、当日を含む） |
| `team_id` | integer | no | 全体 | 指定時はそのチームのみ |

**レスポンス例 (200)**

```json
{
  "...": "..."
}
```

**エラーレスポンス**

| ステータス | 条件 | ボディ |
|---|---|---|
| 400 | パラメータ不正 | `{"error": {"code": "invalid_parameter", "message": "..."}}` |
| 404 | 対象が存在しない | `{"error": {"code": "not_found", "message": "..."}}` |

---

## 4. バックエンドの構成

| ファイル | 責務 |
|---|---|
| `api/app/models/<model>.rb` | |
| `api/app/services/github/<service>.rb` | |
| `api/app/metrics/<calculator>.rb` | |
| `api/app/serializers/<serializer>.rb` | |
| `api/app/controllers/api/v1/<controller>.rb` | |
| `api/app/jobs/<job>.rb` | |

> [CLAUDE.md](../../../CLAUDE.md) の「8.1 Rails」の規約に従うこと。
> 特に: コントローラ・モデルから直接 Octokit を呼ばない／集計は SQL 側に寄せる。

---

## 5. 指標計算アルゴリズム

計算の手順を、境界条件の扱いまで含めて具体的に書きます。
定義そのものは [docs/glossary.md](../../glossary.md) が出典です。ここでは**実装方法**を書きます。

```
<疑似コードまたは SQL>
```

**境界条件の扱い**

| ケース | 扱い |
|---|---|
| 対象データ0件 | `null` を返す |
| <...> | |

---

## 6. フロントエンドの構成

| ファイル | 責務 |
|---|---|
| `web/src/api/types.ts` | レスポンス型の追加 |
| `web/src/features/<feature>/<Component>.tsx` | |
| `web/src/components/charts/<Chart>.tsx` | |
| `web/src/lib/<transform>.ts` | データ整形の純関数（テスト対象） |

**画面構成**

<レイアウト、表示要素、ローディング・エラー・データなしの各状態の見せ方。>

> [CLAUDE.md](../../../CLAUDE.md) の「8.2 React」の規約に従うこと。
> 特に: データ整形はコンポーネント外の純関数に切り出す／`any` 禁止。

---

## 7. エラー処理

| 発生箇所 | 条件 | 挙動 |
|---|---|---|
| GitHub API | レート制限 (403) | リセット時刻まで待機してリトライ。上限超過時はジョブを失敗させログに残す |
| GitHub API | タイムアウト | 指数バックオフで N 回リトライ |
| 取り込み | データ不整合 | 該当レコードをスキップし、警告ログに件数を残す。処理全体は止めない |
| API | 不正なパラメータ | 400 を返す |

---

## 8. テスト戦略

> **TDD で実装します。ここに書いたテストを先に書きます。**

### 先に書くテスト一覧

| # | テストファイル | ケース | 対応する受入基準 |
|---|---|---|---|
| T-1 | `api/spec/metrics/<x>_spec.rb` | <...> | AC-1 |
| T-2 | `api/spec/metrics/<x>_spec.rb` | 対象データが0件のとき `null` を返す | AC-N |
| T-3 | `api/spec/requests/api/v1/<x>_spec.rb` | <...> | AC-2 |
| T-4 | `web/src/lib/<x>.test.ts` | <...> | AC-3 |

### テストデータ

- **FactoryBot**: 追加・変更する factory
- **VCR カセット**: 記録する GitHub API のリクエスト（実 API はテスト中に叩かない）

### カバーする境界値

指標計算を含む場合、[CLAUDE.md](../../../CLAUDE.md) の「3.3」に挙げた境界を必ずカバーすること。

- [ ] デプロイが0件の期間
- [ ] 障害が0件の期間
- [ ] 集計期間をまたぐ PR
- [ ] チームラベルが付いていない PR
- [ ] 同一 PR に複数レビューがある場合
- [ ] レビューなしでマージされた PR

---

## 9. 検討した代替案

| 案 | 内容 | 採否と理由 |
|---|---|---|
| A | | **採用** — <...> |
| B | | 不採用 — <...> |

---

## 10. 変更履歴

| 日付 | 変更内容 | 理由 |
|---|---|---|
| YYYY-MM-DD | 初版 | |
